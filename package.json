{
  "name": "@hasna/workforce",
  "version": "0.1.0",
  "description": "Unified roster/HRIS: humans, contractors and agents as first-class org members — CLI + MCP + serve over a Hasna-contract store.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "workforce": "dist/cli/index.js",
    "workforce-mcp": "dist/mcp/index.js",
    "workforce-serve": "dist/server/index.js"
  },
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }
  },
  "files": ["dist", "hasna.contract.json", "openapi.json", "docker-compose.yml", "LICENSE", "README.md"],
  "scripts": {
    "build": "bun build src/cli/index.tsx --outdir dist/cli --target bun --external ink --external react --external chalk --external @modelcontextprotocol/sdk --external hono --external pg && bun build src/mcp/index.ts --outdir dist/mcp --target bun --external @modelcontextprotocol/sdk --external pg && bun build src/server/index.ts --outdir dist/server --target bun --external hono --external pg && bun build src/index.ts --outdir dist --target bun --external pg && tsc --emitDeclarationOnly --outDir dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "conformance": "bun run scripts/conformance.ts",
    "verify": "bun run typecheck && bun run test && bun run build && bun run conformance",
    "openapi:generate": "bun run src/cli/index.tsx openapi generate --out openapi.json",
    "openapi:check": "bun run src/cli/index.tsx openapi check --path openapi.json",
    "smoke:package": "bun run src/release/package-smoke.ts",
    "dev:cli": "bun run src/cli/index.tsx",
    "dev:mcp": "bun run src/mcp/index.ts --http --port 8888",
    "dev:serve": "bun run src/server/index.ts",
    "postinstall": "node -e \"try{const os=require('os'),fs=require('fs'),p=require('path');const b=p.join(os.homedir(),'.hasna','workforce');for(const d of ['','config','data','exports','backups','logs','tmp']) fs.mkdirSync(p.join(b,d),{recursive:true});}catch(e){}\""
  },
  "publishConfig": { "registry": "https://registry.npmjs.org", "access": "public" },
  "repository": { "type": "git", "url": "git+https://github.com/hasna/workforce.git" },
  "homepage": "https://github.com/hasna/workforce#readme",
  "bugs": { "url": "https://github.com/hasna/workforce/issues" },
  "engines": { "bun": ">=1.1.0" },
  "author": "Andrei Hasna <andrei@hasna.com>",
  "license": "Apache-2.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "hono": "^4.6.0",
    "commander": "^13.1.0",
    "ink": "^5.2.0",
    "react": "^18.3.1",
    "chalk": "^5.4.1",
    "zod": "^3.24.2",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@hasna/contracts": "0.4.1",
    "@types/bun": "^1.2.4",
    "@types/pg": "^8.11.10",
    "@types/react": "^18.3.18",
    "typescript": "^5.7.3"
  }
}
